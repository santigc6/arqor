Autores: David García Fernández y Santiago González- Carvajal Centenera.

-El código necesario para ejecutar el script de cada ejercicio va incluido en el directorio correspondiente.

-En el directorio de cada ejercicio se incluye un directorio con sus correspondientes resultados. 

Nota: los resultados cuyo valor de P no aparece explícitamente en el nombre de la carpeta que los contiene han sido obtenidos con el valor de P correspodiente
a nuestro número de pareja. Se incluyen resultados con distintos valores de P para demostrar la corrección del ejercicio (cuando es necesario).

-Para el ejercicio 2 solo es necesario realizar una repetición, ya que la simulación de tamaño de memoria caché de valgrind, siempre obtiene el mismo número de
fallos cada vez que se ejecuta con los mismo parámetros.
